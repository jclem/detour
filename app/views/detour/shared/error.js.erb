if ($(".modal-body .panel-danger").length) {
  $(".modal-body .panel-danger").replaceWith("<%= j render partial: "detour/shared/errors", locals: { model: @model } %>");
} else {
  $(".modal-body").prepend("<%= j render partial: "detour/shared/errors", locals: { model: @model } %>");
}
