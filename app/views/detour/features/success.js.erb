$("#create-feature").modal("hide");

if ($(".alert-success").length) {
  $(".alert-success").replaceWith("<%= j render "success" %>");
} else {
  $("h1").before("<%= j render "success" %>");
}

$(window).scrollTop(0);
